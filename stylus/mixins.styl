border-radius(radius = 5px)
    -webkit-border-radius radius
    border-radius radius

clear-fix()
    zoom 1
    &:after
    &:before
        content ""
        display table
    &:after
        clear both   
